# FINDINGS — proshop_mern

| # | Риск | Где | Что | Как фиксить | Статус |
|---|------|-----|-----|-------------|--------|
| 1 | 🔴 | `backend/models/userModel.js::pre('save')` | После `if (!this.isModified('password')) { next() }` нет `return` — выполнение продолжается, и при любом `user.save()` без смены пароля уже захэшированное значение хэшируется ещё раз; после такого `updateUserProfile`/`updateUser` логин ломается, т.к. `bcrypt.compare(plaintext, hash(hash))` всегда `false`. | Заменить на `if (!this.isModified('password')) return next()`. | ✅ fixed in [c936c41](../../commit/c936c41) |
| 2 | 🔴 | `backend/controllers/orderController.js::addOrderItems` | Сервер сохраняет `itemsPrice`, `taxPrice`, `shippingPrice`, `totalPrice` напрямую из `req.body` — клиент может прислать любой `totalPrice` (например, 0.01) и оплатить заказ за бесценок. | Пересчитывать все цены на сервере из `Product.findById` по каждому `orderItem`, игнорируя клиентские суммы. | 🔴 not yet |
| 3 | 🔴 | `package.json` + `frontend/package.json` | Зависимости с известными CVE и заброшенные: `jsonwebtoken@8` (CVE-2022-23529), `mongoose@5.10` (CVE-2024-53900, до 5.13.20), `axios@0.20` (CVE-2021-3749 SSRF/CRLF), `react-scripts@3.4.3` (Webpack 4 + десятки уязвимостей), `react@16.13`, `react-paypal-button-v2` (заброшен, deprecated PayPal SDK v4). | Поднять стек: `mongoose@8` (с переписыванием `config/db.js`), `jsonwebtoken@9`, `axios@1.x`, `react-scripts@5` или Vite, заменить `react-paypal-button-v2` на `@paypal/react-paypal-js`. | 🔴 not yet |
| 4 | 🟡 | `backend/controllers/orderController.js::addOrderItems` | Проверка `if (orderItems && orderItems.length === 0)` пропускает `null`/`undefined` — при пустом теле запроса заказ всё равно создаётся (с невалидной схемой → 500 вместо 400). Плюс `return` после `throw new Error('No order items')` — мёртвый код. | Заменить на `if (!orderItems || orderItems.length === 0) { res.status(400); throw new Error('No order items') }` и убрать `else` + `return`. | 🔴 not yet |
| 5 | 🟡 | `frontend/src/screens/PlaceOrderScreen.js` (тело компонента) | Прямая мутация redux-state: `cart.itemsPrice = ...`, `cart.totalPrice = ...` пишет в объект из `useSelector`. Плюс после `history.push('/shipping')` нет `return` — расчёты дальше выполняются на пустых полях. | Считать суммы в локальные `const` (или мемо), отдавать в `dispatch(createOrder(...))`; добавить `return` после `history.push`. | 🔴 not yet |
| 6 | 🟡 | `frontend/src/screens/PlaceOrderScreen.js` (calc prices) | Магические числа `0.15` (НДС), `100` (порог free shipping), `100` (стоимость доставки) захардкожены в UI-слое; ставка налога и тарифы доставки должны прийти с сервера/конфига и применяться на бэке. | Вынести в `backend/config/pricing.js` (или ENV) и считать на сервере в `addOrderItems`; фронт отображает то, что вернул API. | 🔴 not yet |
| 7 | 🟡 | `frontend/src/screens/OrderScreen.js` (тело + `useEffect`) | `if (!loading) { order.orderItems.reduce(...) }` упадёт по TypeError, когда `loading=false` и `order=null` (например, на ошибке загрузки). Плюс `addPayPalScript` каждый раз делает `document.body.appendChild(script)` без очистки в `useEffect` cleanup → утечка `<script>` тегов и слушателей. | Защитить блок `if (!loading && order) {...}`; в `useEffect` возвращать cleanup, удаляющий вставленный `<script>`. | 🔴 not yet |
